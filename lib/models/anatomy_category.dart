import 'package:flutter/material.dart';

class AnatomyCategory {
  final String level;
  final String title;
  final int progress;
  final IconData icon;
  final int color;

  AnatomyCategory(this.level, this.title, this.progress, this.icon, this.color);
}